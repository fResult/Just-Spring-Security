spring:
  application:
    name: "Just Spring Security (Test)"

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

# Keep users here for learning purposes
app:
  config:
    users:
      - username: test1
        password: password
        roles:
          - USER
      - username: test2
        password: password
        roles:
          - USER
      - username: admintest
        password: password
        roles:
          - ADMIN
