spring:
  application:
    name: "Just Spring Security"

  jpa:
    show-sql: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

logging:
  level:
    org:
      springframework:
        data:
          jpa: TRACE
