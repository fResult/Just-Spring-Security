spring:
  application:
    name: Spring Cloud Config Client
  config:
    import: "optional:configserver:http://localhost:8888"

management:
  endpoints:
    web:
      exposure:
        include: refresh
